/**
 * Kept as a re-export so existing imports keep working. The list itself lives
 * in `algeria.ts` — see the note there about why there must only be one.
 */
export { WILAYAS } from "./algeria";
export type { Wilaya } from "./algeria";
